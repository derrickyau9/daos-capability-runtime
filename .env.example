# API provider: export these variables in your shell (never commit a real key).
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini
# Alternative: --provider codex uses your own existing `codex login` session.
# CODEX_MODEL may select a model available to that account.
CODEX_MODEL=
PORT=4317
